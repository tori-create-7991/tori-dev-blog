---
title: javascript 配列
description: 'JavaScriptの配列操作(shift/pop/reduce/filter/every/some/includes等)を使い方付きでまとめたメモ'
date: 2021-04-24T00:00:00.000Z
image: ''
tags:
  - javascprit
  - memo
categories:
  - javascript
---

## 操作

```javascript

const  arr = []

// 先頭を削除
arr.shift() ;

// 末尾を削除
arr.pop() ;

// 配列合計
data.reduce((a, x) =>{return a + ((x || 0) - 0);}, 0);

// 配列から新しい配列
const count = Array.from(line)

// 配列を空,null undefined を削除
arr.filter((v) => v
// 配列の要素すべてが条件を満たしているか
  o = s.every(isUpperCase)
//配列の要素一つでも条件を満たしているか
arr.some()
//配列のkeyを取得
arr_key = Object.keys(arr)
// 配列に引数の値が含まれているかどうか
arr_key.includes(x)
//配列の順番を逆に
arr.reverse()

// 配列の重複削除
const array1 = [1, 5, 3, 1, 5, 3];
const array2 = Array.from(new Set(array1))

```
