# nginxのAlpineイメージを使用
FROM nginx:alpine

# 作業ディレクトリを設定
WORKDIR /usr/share/nginx/html

# nginx設定ファイルをコピー
COPY nginx.conf /etc/nginx/conf.d/default.conf

# ポート8080を公開
EXPOSE 8080

# nginxをフォアグラウンドで実行
CMD ["nginx", "-g", "daemon off;"]
